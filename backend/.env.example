## Supabase hosted projects (server-side / scripts only)
# Never expose service role keys in frontend code.

## Live (production project)
SUPABASE_LIVE_URL=
SUPABASE_LIVE_SERVICE_ROLE_KEY=

## Azure Document Intelligence (Edge Functions)
AZURE_DOCINT_ENDPOINT=
AZURE_DOCINT_KEY=

## Edge Function shared tokens (optional but recommended)
TEST_DOCUMENT_UPLOAD_TOKEN=
PROCESS_DOCUMENT_TOKEN=
