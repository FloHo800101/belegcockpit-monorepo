# Offline Mass Test Harness (Doc Lifecycle)

Runs doc-lifecycle assessments without Supabase.

## Run

- `pnpm matching:mass_doc:offline`
- Optional: `pnpm matching:mass_doc:offline:dataset -- --dataset <path> --out <dir>`
