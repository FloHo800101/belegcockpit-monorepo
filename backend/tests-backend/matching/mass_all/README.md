# Offline Mass Test Harness (All)

Runs the full pipeline (matching + doc lifecycle + tx lifecycle) without Supabase.

## Run

- `pnpm matching:mass_all:offline`
- Optional: `pnpm matching:mass_all:offline:dataset -- --dataset <path> --out <dir>`
