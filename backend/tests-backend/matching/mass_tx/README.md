# Offline Mass Test Harness (Tx Lifecycle)

Runs tx-lifecycle assessments without Supabase.

## Run

- `pnpm matching:mass_tx:offline`
- Optional: `pnpm matching:mass_tx:offline:dataset -- --dataset <path> --out <dir>`
